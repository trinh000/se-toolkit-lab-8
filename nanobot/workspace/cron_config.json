{
  "jobs": [
    {
      "id": "health-check",
      "schedule": "*/15 * * * *",
      "enabled": true,
      "description": "Periodic health check - checks backend errors and posts summary",
      "prompt": "Check the backend for errors in the last 15 minutes using observability tools. Query VictoriaLogs for recent errors and check backend health. Post a summary report here with: 1) VictoriaLogs health status, 2) Any errors found, 3) Backend health status."
    }
  ]
}
